
name: Ejemplo de GitHub Actions con Parámetros

on:  
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      app:
        description: 'Aplicación a desplegar o destruir'
        required: true
        default: 'deploy'
        options:
          - deploy
          - destroy

jobs:
  ejemplo_job:
    runs-on: ubuntu-latest

    steps:
      - name: Imprimir Parámetro app
        run: echo "Aplicación seleccionada:${{ jobs.ejemplo_job.steps.app }}" # Agregamos espacio entre las palabras


        

        


  
  
